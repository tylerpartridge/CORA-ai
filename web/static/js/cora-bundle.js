/* CORA Core Bundle - marker only
 * Avoid dynamic loading to prevent race conditions.
 * Core scripts are loaded explicitly via template tags.
 */
(function(){
  window.CORA_BUNDLE_LOADED = true;
})();

