param()
$script = "tools/dev_verify_root.ps1"
if (Test-Path $script) {
  Write-Host "[pre-push] running $script"
  powershell -ExecutionPolicy Bypass -File $script
} else {
  Write-Host "[pre-push] $script not found; skipping check."
}
