[pytest]
addopts = -q --maxfail=1 --disable-warnings --ignore=tests/test_endpoints.py --ignore=tests/security_test.py
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    e2e: end-to-end tests that hit a live server
